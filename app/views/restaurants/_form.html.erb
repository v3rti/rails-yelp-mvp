<%= simple_form_for(restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.select :category, options_for_select(%w[japanese french belgian chinese italian])%>
  <%= f.submit %>
<% end %>
